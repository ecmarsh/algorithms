const HashTable = require( './HashTable' );
const { Stack, accessStack, stackHas } = require( './Stack' );
const Queue = require( './Queue' );

module.exports = { HashTable, Stack, accessStack, stackHas, Queue };
